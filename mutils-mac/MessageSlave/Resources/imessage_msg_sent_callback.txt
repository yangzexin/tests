using terms from application "Messages"	on message sent theMessage for theBuddy		delay (0)		--display dialog (get id of theBuddy as string)		tell application "System Events"			set the clipboard to (get id of theBuddy as string)			tell application "MessageSlave"				activate			end tell			tell process "MessageSlave"				keystroke "i" using {command down}			end tell		end tell	end message sentend using terms from